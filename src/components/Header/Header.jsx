import React from "react";
import { Container, Typography } from "@material-ui/core";

const Header = () => {
  return (
    <Container className="maxWidth">
      <Typography variant="h1">Projectory</Typography>
    </Container>
  );
};

export default Header;
